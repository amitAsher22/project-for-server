const { checkout} = require('./checkout');
const {hello} = require('./hello')
const {hi} = require('./hi')
const {amit} = require('./amit')

module.exports = {
    checkout,
    hello,
    hi,
    amit,
};
